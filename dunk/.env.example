# Example env for running import scripts locally
# Option A (recommended): provide the service account JSON directly as an env var (raw JSON or base64-encoded):
# SERVICE_ACCOUNT_JSON='{"type":...}'
# or
# SERVICE_ACCOUNT_JSON=<base64 of the JSON file>

# Option B: point to a local file (make sure it's in .gitignore):
# SERVICE_ACCOUNT=./serviceAccountKey.json

# Firebase Realtime Database URL (optional if in service account JSON)
# FIREBASE_DATABASE_URL=https://your-project-id-default-rtdb.firebaseio.com

# Example (PowerShell) - DO NOT COMMIT real values:
# $env:SERVICE_ACCOUNT_JSON = Get-Content -Raw .\serviceAccountKey.json
# $env:FIREBASE_DATABASE_URL = 'https://your-project-id-default-rtdb.firebaseio.com'
